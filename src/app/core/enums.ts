export enum FilterOptionValues {
    ALL,
    COMPLETED,
    PENDING,
    PRIORITY_HIGH,
    PRIORITY_MEDIUM,
    PRIORITY_LOW,
    NO_PRIORITY
};

export enum SortOptionsValues {
    RECENT,
    OLDEST
}
